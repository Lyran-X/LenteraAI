# Archive

This folder stores legacy project artifacts that are kept for reference only.

- `schema.txt` is an archived database schema snapshot from an earlier design phase.
- It is not used by the FastAPI runtime.
- It is not used by Alembic migrations.
- It is not used by the frontend or active backend services.
- The active database schema is managed through SQLAlchemy models and Alembic migrations.
